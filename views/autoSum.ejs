<% include ./partials/header %>
<div class="container">
    <h2 align="center">Σύνολο Εξαρτημάτων Αυτόματων</h2>
    <table class="table">
        <thead class="table-dark">
            <tr>
                <th scope="col">ΚΑΤΗΓΟΡΙΑ</th>
                <th scope="col">ΤΥΠΟΣ</th>
                <th scope="col">ΣΥΝΟΛΟ</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <th scope="row">ΗΛΙΑΚΗ ΓΕΝΝΗΤΡΙΑ</th>
                <% var first_iteration = true; %>
                <% for (const [key, value] of Object.entries(solarGeneratorsCount)) { %>
                    <% if(first_iteration == true) { %>
                        <td>
                            <%= key %>
                        </td>
                        <td>
                            <%= value %>
                        </td>
                        <% first_iteration = false; %>
                    <% } else if (key != "") { %>
                        <tr>
                            <td>
                                
                            </td>
                            <td>
                                <%= key %>
                            </td>
                            <td>
                                <%= value %>
                            </td>
                        </tr>
                    <% } %>
                <% } %>
            </tr>
            <tr>
                <th scope="row">ΛΥΧΝΙΑ</th>
                <% var first_iteration = true; %>
                <% for (const [key, value] of Object.entries(lampsCount)) { %>
                    <% if(first_iteration == true) { %>
                        <td>
                            <%= key %>
                        </td>
                        <td>
                            <%= value %>
                        </td>
                        <% first_iteration = false; %>
                    <% } else if (key != "") { %>
                        <tr>
                            <td>
                                
                            </td>
                            <td>
                                <%= key %>
                            </td>
                            <td>
                                <%= value %>
                            </td>
                        </tr>
                    <% } %>
                <% } %>
            </tr>
            <tr>
                <th scope="row">ΣΥΣΣΩΡΕΥΤΗΣ</th>
                <% var first_iteration = true; %>
                <% for (const [key, value] of Object.entries(accumulatorsCount)) { %>
                    <% if(first_iteration == true) { %>
                        <td>
                            <%= key %>
                        </td>
                        <td>
                            <%= value %>
                        </td>
                        <% first_iteration = false; %>
                    <% } else if (key != "") { %>
                        <tr>
                            <td>
                                
                            </td>
                            <td>
                                <%= key %>
                            </td>
                            <td>
                                <%= value %>
                            </td>
                        </tr>
                    <% } %>
                <% } %>
            </tr>
            <tr>
                <th scope="row">ΡΥΘΜΙΣΤΗΣ ΦΟΡΤΙΣΕΩΣ</th>
                <% var first_iteration = true; %>
                <% for (const [key, value] of Object.entries(generatorSocketsCount)) { %>
                    <% if(first_iteration == true) { %>
                        <td>
                            <%= key %>
                        </td>
                        <td>
                            <%= value %>
                        </td>
                        <% first_iteration = false; %>
                    <% } else if (key != "") { %>
                        <tr>
                            <td>
                                
                            </td>
                            <td>
                                <%= key %>
                            </td>
                            <td>
                                <%= value %>
                            </td>
                        </tr>
                    <% } %>
                <% } %>
            </tr>
            <tr>
                <th scope="row">ΦΟΡΤΙΣΤΗΣ</th>
                <% var first_iteration = true; %>
                <% for (const [key, value] of Object.entries(socketsCount)) { %>
                    <% if(first_iteration == true) { %>
                        <td>
                            <%= key %>
                        </td>
                        <td>
                            <%= value %>
                        </td>
                        <% first_iteration = false; %>
                    <% } else if (key != "") { %>
                        <tr>
                            <td>
                                
                            </td>
                            <td>
                                <%= key %>
                            </td>
                            <td>
                                <%= value %>
                            </td>
                        </tr>
                    <% } %>
                <% } %>
            </tr>
        </tbody>
    </table>
    <div class="col text-center">
        <a href="/" class="btn btn-outline-primary">Αρχική Σελίδα</a>
    </div>
</div>

<br>

<% include ./partials/footer %>